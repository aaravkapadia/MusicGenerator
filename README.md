# MusicGenerator
This project is LTSM neural network that is trained on thousands of Irish Folk Piano Songs. The network batch vectorizes the .abc file that contains the music, trains on that data with Cross Entropy Loss,
and is able to generate a playable midi file that contains an AI- generated Irish Folk Piano Song. 
